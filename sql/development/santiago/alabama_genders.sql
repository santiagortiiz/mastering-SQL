SELECT 
	state, zip_code, total_population, sex, 
	percent_population_for_sex, --total_category, 
	food_at_home, food_away_from_home, total_food_expenditure
FROM chunk
;

SELECT * FROM alabama;
SELECT *
FROM alabama_genders;
